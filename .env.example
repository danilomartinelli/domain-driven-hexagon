# ==================================================
# DOMAIN-DRIVEN HEXAGON - ENVIRONMENT CONFIGURATION
# ==================================================
# This file contains all required environment variables
# Copy this file to .env and update the values

# ==================================================
# APPLICATION CONFIGURATION
# ==================================================
NODE_ENV=development
PORT=3000

# ==================================================
# DATABASE CONFIGURATION
# ==================================================
# Required database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_db_username
DB_PASSWORD=your_secure_db_password_min_8_chars
DB_NAME=your_database_name

# SSL Configuration
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true

# Connection Pool Settings (Optional - defaults provided)
DB_MAX_POOL_SIZE=20
DB_MIN_POOL_SIZE=5
DB_ACQUIRE_TIMEOUT=30000
DB_CREATE_TIMEOUT=30000
DB_DESTROY_TIMEOUT=5000
DB_IDLE_TIMEOUT=300000
DB_REAP_INTERVAL=1000
DB_CREATE_RETRY_INTERVAL=200

# Query Timeout Settings (Optional - defaults provided)
DB_CONNECTION_TIMEOUT=30000
DB_STATEMENT_TIMEOUT=60000
DB_QUERY_TIMEOUT=30000

# Health Check Configuration (Optional - defaults provided)
DB_HEALTH_CHECK_INTERVAL=30000

# Migration Settings (Optional - defaults provided)
DB_MIGRATION_TABLE=migration
DB_MIGRATIONS_PATH=./database/migrations

# Database Logging (Optional - defaults provided)
DB_LOG_LEVEL=info
DB_ENABLE_QUERY_LOGGING=false

# ==================================================
# SECURITY CONFIGURATION
# ==================================================
# JWT Configuration (Optional - only if using JWT)
JWT_SECRET=your_super_secure_jwt_secret_min_32_chars

# Encryption Configuration (Optional - only if using encryption)
ENCRYPTION_KEY=your_super_secure_encryption_key_min_32_chars

# Session Configuration (Optional - only if using sessions)
SESSION_SECRET=your_super_secure_session_secret_min_32_chars

# Rate Limiting Configuration
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=100

# Security Headers Configuration
CSP_ENABLED=true
HSTS_MAX_AGE=31536000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=false

# Logging Configuration
LOG_LEVEL=info
LOG_SECURITY_EVENTS=true

# ==================================================
# SECURITY NOTES
# ==================================================
# 1. NEVER commit this file with real credentials to version control
# 2. Use strong passwords with at least 12 characters
# 3. Include uppercase, lowercase, numbers, and special characters
# 4. For production, use environment-specific secrets management
# 5. Regularly rotate all secrets and passwords
# 6. Enable SSL/TLS in production (DB_SSL=true)
# 7. Use proper CORS origins in production (not localhost)
# 8. Set LOG_LEVEL=warn or error in production
# 9. Consider using external secret management services like:
#    - AWS Secrets Manager
#    - Azure Key Vault  
#    - HashiCorp Vault
#    - Docker secrets
#    - Kubernetes secrets

# ==================================================
# PRODUCTION ENVIRONMENT CHECKLIST
# ==================================================
# □ NODE_ENV=production
# □ Strong, unique passwords (12+ characters)
# □ DB_SSL=true with valid certificates
# □ Proper CORS_ORIGIN (not localhost)
# □ LOG_LEVEL=warn or error
# □ All secrets stored in secure secret management
# □ Regular secret rotation schedule
# □ Database user has minimal required permissions
# □ Network access restrictions in place
# □ Monitoring and alerting configured

# ==================================================
# EXAMPLE PRODUCTION VALUES (REPLACE WITH REAL VALUES)
# ==================================================
# NODE_ENV=production
# PORT=3000
# DB_HOST=your-production-db-host.com
# DB_PORT=5432
# DB_USERNAME=app_user
# DB_PASSWORD=Y0ur$uper$ecureP@ssw0rd2024!
# DB_NAME=production_db
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=true
# JWT_SECRET=a-very-long-and-random-jwt-secret-string-min-32-characters
# ENCRYPTION_KEY=another-very-long-and-random-encryption-key-min-32-chars
# CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
# LOG_LEVEL=warn